# Firebase Realtime Database — copiá esto a .env.local y completá los valores.
# Obtené los valores en: Firebase Console → tu proyecto → Configuración del proyecto → Tus apps (web) → SDK setup

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_DATABASE_URL=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
